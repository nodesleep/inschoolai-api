import express from "express";
import { findStudent, removeStudent } from "../models/student.js";

const router = express.Router();

// Future student-specific endpoints can be added here

export default router;
